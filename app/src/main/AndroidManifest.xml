<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.am.gethelp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".DrawerActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />


        <activity
            android:name=".TimelineSimple"
            android:label="تفاصيل المشاركة" />

        <activity
            android:name="SplashActivity"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ChatFacebook"
            android:screenOrientation="portrait" />

        <activity
            android:name=".CaseActivity"
            android:label="التبليغ عن حادثة" />
        <activity
            android:name=".EmrgencyBtnActivity"
            android:label="طلب المساعدة الطارئة" />
        <activity
            android:name=".ContactActivity"
            android:label="من نحن" />
        <activity
            android:name=".ArticleSimple"
            android:label="تفاصيل الخبرء" />
        <activity
            android:name=".LoginActivity"
            android:label="تسجيل الدخول" />


    </application>

</manifest>